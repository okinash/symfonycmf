{% extends 'EventsBundle::layout.html.twig' %}

{% block main %}
    <h1>Events Overview</h1>
    <div><ul>
            {% for event in cmf_children(cmf_find('/cms/simple/events')) %}
                        <li><a href="/events/{{ event.name }}">{{ event.title }}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
